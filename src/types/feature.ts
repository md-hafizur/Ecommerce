import { ComponentType } from "react";

export interface Feature {
//   icon: ComponentType<{ className?: string }>; // React component (e.g., CreditCard)
  image: string;
  title: string;
  description: string;
}
